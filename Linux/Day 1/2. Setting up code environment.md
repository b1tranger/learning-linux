> using Ubuntu

### # note:
- terminal responses copy and pasted as html (see `<pre></pre>` lines in the code view)
- Before installing any package, it’s a good practice to update the package list so that you get the latest version available. [link](https://gcore.com/learning/how-to-install-curl-on-ubuntu)
  `sudo apt update`
-

### # commands:
- installing Antigravity (instead of VS code) first:
  - instruction: Add the repository to sources.list.d  <br>
    ```
    sudo mkdir -p /etc/apt/keyrings
    curl -fsSL https://us-central1-apt.pkg.dev/doc/repo-signing-key.gpg | \
    sudo gpg --dearmor --yes -o /etc/apt/keyrings/antigravity-repo-key.gpg
    echo "deb [signed-by=/etc/apt/keyrings/antigravity-repo-key.gpg] https://us-central1-apt.pkg.dev/projects/antigravity-auto-updater-dev/ antigravity-debian main" | \
    sudo tee /etc/apt/sources.list.d/antigravity.list > /dev/null
    ```
    <pre>Command &apos;curl&apos; not found, but can be installed with:
    sudo snap install curl  # version 8.17.0, or
    sudo apt  install curl  # version 8.5.0-2ubuntu10.6
    See &apos;snap info curl&apos; for additional versions.
    gpg: no valid OpenPGP data found.</pre>

 
    > https://gcore.com/learning/how-to-install-curl-on-ubuntu
    
    What is Curl Command in Linux?

    Curl (Client URL) is a command-line tool used for transferring data with various protocols, including HTTP, HTTPS, FTP, and many more. <br>
    Ubuntu, being one of the most popular Linux distributions, often has curl available in its software repositories.  <br>
    This allows users to easily install and update curl using Ubuntu’s package management tools like apt. <br>
    
      
    
    
### # Source:
- https://snapcraft.io/curl
- https://gcore.com/learning/how-to-install-curl-on-ubuntu
