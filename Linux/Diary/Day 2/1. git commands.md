> using Ubuntu

### # note:
- tried using https://vscode.dev/
- git commands cheatsheet [PDF](https://github.com/b1tranger/learning-linux/blob/main/PDF/git-cheat-sheet-education.pdf)

### # commands:
   > I want to use git on linux vs code. How do I get started?

  Ensure Git is Installed on Linux 
  ```
  git --version
  ```
  ```
  sudo apt install git-all
  ```
  Configure Git User Information
  ```
  git config --global user.name "Your Name"
  git config --global user.email "your.email@example.com"
  ```
  Install the Git Extension in VS Code
  
  Open a Git Repository in VS Code

  > Tried to commit and push
  <pre>
  remote: error: GH007: Your push would publish a private email address.        
  remote: You can make your email public or disable this protection by visiting:        
  remote: https://github.com/settings/emails  
  ! [remote rejected] main -> main (push declined due to email privacy restrictions)
  </pre>
  ```
   cd /home/saraf/Downloads/Repositories/learning-linux
  ```
   To set the no-reply email globally for all your repositories, open your terminal or Git Bash and run:
   ```
   git config --global user.email "63799660+b1tranger@users.noreply.github.com"
   ```
   Amend the last commit: The problematic email is in your most recent commit. To reset the author information for the last commit, use:
   ```
   git commit --amend --reset-author --no-edit
   ```
   ```
   git push
   ```

  
  
  
### # Source:
- Google AI Mode
- https://vscode.dev/
- https://education.github.com/git-cheat-sheet-education.pdf
